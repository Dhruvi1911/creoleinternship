// export function fun1()
// {
//     console.log("hii");
// }
// export const a=1;

  function message(){
    const name = "Jesse";
    const age = 40;
    return name + ' is ' + age + 'years old.';
  
    };
   export {message}
    
    // <!-- for import the module we have to run the file in the local server 
    // to start the local sever :=
    // 1.open cmd from the file u want to run
    // 2.run the command python -m http.server
    // 3.then in browser http://localhost:8000/filenmae.html -->
   